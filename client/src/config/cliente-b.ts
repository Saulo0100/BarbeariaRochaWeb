import type { AppConfig } from "./types";

const clienteB: AppConfig = {
  nome: "Barbearia Exemplo",
  logo: "/logo-cliente-b.png",
  tema: {
    primaryColor: "oklch(0.65 0.18 220)",
    secondaryColor: "oklch(0.52 0.15 215)",
  },
};

export default clienteB;
